import React, {useState, useEffect} from 'react'

const App = (props) => {
    // const [state, setState] = useState(defaultState);
    
    // useEffect(() => {
    
    // }, [dependencies]);
    
    return <div></div>
}

export default App;